## Selected-metric API requests

- Skip GitHub Release and asset requests when `downloads` is not selected.
- Preserve previously tracked download totals and asset state while download collection is disabled.
- Prevent unrelated release API failures from blocking dashboards that only use stars, forks, or Git clones.
